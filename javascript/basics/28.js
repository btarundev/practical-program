// JavaScript practical question 28
function calculate(a, operator, b) { if (operator === "+") return a + b; if (operator === "-") return a - b; if (operator === "*") return a * b; return a / b; } console.log(calculate(8, "+", 2));

